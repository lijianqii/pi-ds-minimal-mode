{
  "name": "pi-ds-minimal-mode",
  "version": "1.0.0",
  "description": "Pi coding agent extension mirroring the DeepSeek Harness minimal preset — fixed system prompt + first-request read/bash restriction, active only on deepseek-v4-flash / deepseek-v4-pro.",
  "keywords": ["pi-package"],
  "license": "MIT",
  "pi": {
    "extensions": ["./extensions/ds-minimal-mode.ts"]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  }
}
